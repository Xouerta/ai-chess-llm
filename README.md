# ai-chess-llm
A chess model based on neural network for confrontation generation needs to deploy large language model locally
- py version needs 3.11 below
- change the links in the ./modelconfig.yaml
- you can use the command below:
```
  pip install -r requirements.txt
  py chess_game.py
```
  to start the game ,remember to activate the local model 
